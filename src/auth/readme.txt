/isAuthenticated
takes param id which is the session token
outputs JSON where id is the user id
authenticated: true/false
/Authenticate
takes params username and password
returns 
nothing if failed
a session id as token
a user id as id
and experation date as expiration 